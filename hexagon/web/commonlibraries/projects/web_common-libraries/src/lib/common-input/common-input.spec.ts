import { CommonInputComponent } from './common-input.component';

describe('CommonInputComponent', () => {
    let component: CommonInputComponent;

    beforeEach(() => {
        component = new CommonInputComponent();
    });

    it('should create', () => {
        // Assert
        expect(component).toBeTruthy();
    });
});
