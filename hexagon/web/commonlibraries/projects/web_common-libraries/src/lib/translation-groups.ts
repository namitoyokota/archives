/**
 * List of translation groups
 */
export enum TranslationGroup {
  core = 'commonlibraries-core', // Tokens that are in the capability manifest
  main = 'commonlibraries-main', // Tokens that are needed by the main product: views, adapter components, injectable components
}
