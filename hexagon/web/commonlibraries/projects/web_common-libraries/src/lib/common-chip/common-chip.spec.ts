import { CommonChipComponent } from './common-chip.component';

describe('CommonChipComponent', () => {
    let component: CommonChipComponent;

    beforeEach(() => {
        component = new CommonChipComponent();
    });

    it('should create', () => {
        // Assert
        expect(component).toBeTruthy();
    });
});
