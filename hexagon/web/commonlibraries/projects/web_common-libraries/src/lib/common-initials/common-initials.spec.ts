import { CommonInitialsComponent } from './common-initials.component';

describe('CommonInitialsComponent', () => {
    let component: CommonInitialsComponent;

    beforeEach(() => {
        component = new CommonInitialsComponent();
    });

    it('should create', () => {
        // Assert
        expect(component).toBeTruthy();
    });
});
