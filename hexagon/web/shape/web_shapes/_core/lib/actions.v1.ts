/**
 * Actions that can be performed through the linked view system
 */
export enum Actions$v1 {
  shapeFilter = '@hxgn/shapes/filter',
  filter = '@hxgn/shapes/list/filter', // Shape filter
  filterSync = '@hxgn/shapes/list/filter/sync' // Shape filter sync
}
