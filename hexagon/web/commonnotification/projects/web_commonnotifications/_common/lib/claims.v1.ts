/**
 * Claims that are used by guards. A route or block of
 * content can be hidden based on claims
 */
export enum Claims {
    // {claim} = 'ui{cliam}Access
    notificationManagerAccess = 'uiNotificationManagerAccess'
}
