/**
 * List of translation groups
 */
export enum TranslationGroup {
    common = 'commonNotification-common',
    core = 'commonNotification-core', // Tokens that are in the capability manifest
    main = 'commonNotification-main', // Tokens that are needed by the main product: views, adapter components, injectable components
    notificationManager = 'commonNotification-manager'
}
