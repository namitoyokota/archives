/**
 * Type used to determine the layout for the wizard step.
 */
export enum WizardStepType {
    simple = 'Simple', //  Layout with one column and one row
    split = 'Split' // Layout with two columns
}
