/**
 * The different tabs that can be displayed in onboarding setup page
 */
export enum OnboardingTab$v1 {
    organizationInfo = 0,
    featureFlags = 1,
    recoveryManager = 2,
    viewChangelog = 3
}
