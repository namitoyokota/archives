/**
 * Used to track last performed actions with Supabase
 */
export enum LastAction {
    null = '',
    create = 'Create',
    update = 'Update',
    delete = 'Delete',
    complete = 'Complete',
}
